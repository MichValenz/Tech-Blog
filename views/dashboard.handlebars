

<section>
  <h2>Create New Blog</h2>

  <form class="new-post-form">
    <div>
      <label for="post-title">Title</a></label>
      <input type="text" id="post-title" name="post-title" />
    </div>

    <div>
      <label for="post-text">Blog Post</label>
      <input id="post-text" name="post-text" />
    </div>
    <button type="submit" class="btn">Create</button>
  </form>
</section>

{{#if posts.length}}
<section>
  <h2>Your Blogs</h2>
  
  <ol>
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
  </div>
  <button class="button edit-button is-link is-light"><a href="/dashboard/edit/{{post.id}}">Edit Blog</a> </button>
  </div>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}}

<script src="/javascript/add-blog.js"></script>